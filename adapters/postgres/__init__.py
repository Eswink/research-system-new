"""PostgreSQL adapters (M14 Production Canonical State).

All adapters in this package implement `packages/application/ports/` contracts
with `psycopg[binary]` (sync). Domain/Application never import psycopg.
"""
