---
id: EXP-20260813-001
status: ACTIVE
created_at: 2026-08-13
confidence: 0.5
scope: repository
review_after: 2026-11-11
occurrences: 7
error_signature: ""
supersedes: []
source_refs:
  - "M6 独立复审会话：系统 python 3.11 无 openhands 模块（ModuleNotFoundError，2026-08-13）"
  - "M6 独立复审会话：python -B -c 多行内联脚本输出被 OpenHands banner 混入（2026-08-13，两次独立失败）"
  - "M6 独立复审会话成功路径：sdk_probe_run.py / sdk_probe_events.py 临时文件 + io 重定向"
  - "M7 独立复审会话：python -B -m pytest tests/ → 18 collection errors（ModuleNotFoundError: No module named 'openhands' / 'tenacity'，2026-08-13）"
  - "M7 独立复审会话成功路径：uv run --frozen --no-sync python -B -m pytest tests/ → 988 passed（2026-08-13）"
  - "M0-M7 文档对账会话（2026-08-14）：python -B .cursor/skills/cursor-framework-check/scripts/run_all_checks.py --profile m0 → 系统 python 3.11 下 mypy 缺失、openhands/tenacity 缺失、lint-imports unavailable、17 collection errors（同一签名扩展到 run_all_checks 入口）"
  - "M0-M7 文档对账会话成功路径（2026-08-14）：uv run --frozen --no-sync python -B ... run_all_checks.py --profile m0 → python/tests 989 passed，dependency-boundaries/TypeScript/framework 全 PASS"
  - "SA-1R 会话（2026-08-21）：系统 python -B -m pytest tests/contracts/... → ImportError tests/contracts/registry.py:14（ModuleNotFoundError: No module named 'openhands'），tests/contracts 目录整体 collection 失败 exit 2"
  - "SA-1R 会话成功路径（2026-08-21）：uv run --frozen --no-sync python -B -m pytest tests/contracts ... → 652 passed"
---

# Python 探针/脚本必须用 .venv 的 python 并以临时 .py 文件执行，不用系统 python 与 `-c` 内联

## Problem

- 现象 1：`python -B -c "import openhands; ..."` 报 `ModuleNotFoundError: No module named 'openhands'`——裸 `python` 解析到系统 Python 3.11（`D:\environment\Python\Python311`），而 SDK 只装在项目 `.venv`。
- 现象 2：`python -B -c` 多行内联探针脚本（含 f-string/中文）在 PowerShell 下执行时，SDK 的 ASCII banner 与脚本 stdout 混入同一管道，`Select-String` 过滤结果被 banner 行干扰；且内联脚本无法干净重定向。
- 上下文：OpenHands SDK 行为验证（v1.42.0）、Event 流探针、SDK 异常模型确认；Windows PowerShell + uv 管理项目。
- 影响：反复 4 次独立失败（1 次模块缺失 + 2 次输出污染 + 1 次断言定位困难），阻塞对 SDK 真实行为的确认。

## Solution

- 根因：项目有 `.venv` 但 shell PATH 未优先；`python -c` 内联在 PowerShell 下换行/引号/编码行为不可控；SDK 启动打印 banner 到 stdout（`OPENHANDS_SUPPRESS_BANNER=1` 可关）。M6 会话 4 次观察 + M7 会话 1 次独立复现（跨会话，已触发 capture-learning）。
- 解法：
  1. **首选（与 CI 一致）**：`uv run --frozen --no-sync python -B <command>`——本会话全量 `pytest tests/` 988 passed、`validate_bundle.py` 通过，且不依赖 shell PATH。
  2. 或显式调用 `.venv` 解释器：`& d:\research-system\.venv\Scripts\python.exe -B <script>`；环境定位用 `where.exe python / uv` 先确认。
  3. 探针逻辑写成临时 `.py` 文件（如 `%TEMP%\sdk_probe_*.py`，`sys.stdout` 显式 `io.TextIOWrapper(..., encoding="utf-8", errors="replace")`），再整文件执行；不用 `python -c` 内联多行。
  4. 需要静默时把探针 stdout/stderr 重定向到 `io.StringIO()` 再打印结论行，或用 `$env:OPENHANDS_SUPPRESS_BANNER = "1"`。
- 被拒方案：`python -c` 内联多行（输出污染）；裸 `python`（模块缺失）；对 banner 行做字符过滤（脆弱）。

## Evidence

- source_refs：`d:\research-system\.venv\Scripts\python.exe -B -c "import importlib.metadata as m; print(m.version('openhands-sdk'))"` 成功（1.42.0）；`sdk_probe_run.py`/`sdk_probe_events.py` 临时文件成功输出 `run raised: ConversationRunError` 与事件流 `[created, started, message, message, succeeded]`。
- 如何失效：shell PATH 已优先 venv、或 SDK 移除 banner、或换 bash/zsh 时本条部分不适用。

## 备注

- 本条是经验缓冲，不是工程事实；occurrences=7（M6 会话 4 次 + M7 会话 1 次 + M0-M7 对账会话 1 次 + SA-1R 会话 1 次，跨会话）。
- 2026-08-14 对账会话把同一解法扩展到 `run_all_checks.py --profile m0` 聚合门禁入口：任何 Python 工程命令（pytest / run_all_checks / validator）都必须以 `uv run --frozen --no-sync` 执行，系统 `python` 解析到 3.11 会缺全部 dev 依赖。
- 2026-08-21 SA-1R 会话补充 manifest：修改 `tests/contracts/` 后直接 `python -B -m pytest tests/contracts/...` 会因 `tests/contracts/registry.py` 顶层 import openhands-sdk 使整个目录 collection 失败（exit 2），同类错误扩展到了 contract suite 收集路径。
- 已触发 capture-learning 生成 LEARN-20260813-002（与 EXP-20260813-002 的 lint-imports 签名合并为一个提案：Python 测试/脚本必须经 uv 冻结环境执行）。