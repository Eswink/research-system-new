# Plan Archive

归档目录保存不再需要出现在活动索引中的已关闭任务计划。

- 归档前必须确认任务状态为 `DONE` 或 `CANCELLED`，并保留全部状态历史。
- 移动文件时同步 `ALL_PLAN.md`、复检和工程记忆中的相对链接。
- 归档不允许压缩、重写或删除原始证据。
- `REOPENED` 任务应移回 `plans/tasks/`，并新增复检 attempt。