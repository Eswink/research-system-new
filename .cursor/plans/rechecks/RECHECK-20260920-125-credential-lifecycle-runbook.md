---
id: RECHECK-20260920-125
plan_id: PLAN-20260920-125
attempt: 1
status: COMPLETED
result: PASS_WITH_WARNINGS
created_at: 2026-09-21
completed_at: 2026-09-21
reviewer: root-agent-goal-009-cycle5
baseline_ref: 8d443ad
checked_head: 8d443ad
---

# RECHECK-20260920-125 — 凭据生命周期同源判据（EC-05 复检）

## 检查范围

**不采信实施叙述**：本复检自己跑判据、自己**压**判据、自己核对「文档说的语义」与
「代码实测的语义」是不是同一个东西，并核对**原假设被推翻后**记录是否真的改了。

- **A 判据面**：`tests/architecture/python/test_live_credential_lifecycle_same_source.py`
  是否**真的**在判（不是恒绿、不是空转）；
- **B 语义面**：「构造时快照」是否按**构造方式**分三种钉住（无参 / 传映射 / 注册表），
  且**成对**（旧实例不变 + 新实例变）；
- **C 撤销面**：撤销 ⇒ 门 fail-closed 关且**点名**凭据；注册表面必须 `unregister()`
  （撤销环境变量**不**关该面的门）；
- **D 更正面**：GOAL 的 EC-05 判定细则原文被证伪后，是否**真的**改成了实测语义；
- **E 门禁面**：定向套件 + 全量 m0（CI 同形配置）+ 治理；以及 **CI 的反馈**。

## 检查结果

### A 判据面（20 checks 全绿；并且**压**过两次）

`uv run --frozen --no-sync python -B -m pytest tests/architecture/python/test_live_credential_lifecycle_same_source.py -q -p no:randomly`
⇒ **20 passed**；与既有 runbook 判据同跑 ⇒ **30 passed in 1.26s**。

**第一次压测暴露了判据自己太松（本轮最有价值的发现）**：把 §8 的**撤销行**改写成
「清空或删除来源 ⇒ **每次调用**都读一次环境，所以立即生效」——判据**仍然全绿**。
原因：当时只断言「§8 里出现过『快照』」，而 §8 别处还有这个词 ⇒
**「在场」断言证明不了「这一行说的是对的」**。
处置是**收紧判据**（`assert "新构造" in _row(label)`，逐行判），**不是**把改写放过。

**第二次压测**（收紧之后）：同一处改写 ⇒ **RED**：

```
AssertionError: §8 的 '撤销' 行必须写明生效边界是「新构造」，
否则「改环境变量即生效」这种错说法会重新溜进来
assert '新构造' in '| 撤销 | 清空或删除来源 ⇒ **每次调用**都读一次环境，所以立即生效；…'
```

复原 ⇒ **GREEN**（`30 passed`）；`git diff docs/integration/LIVE_MODEL_RUNBOOK.md`
**只有新增**（`59 insertions, 0 deletions`）。

**判据自己的第二次返工（也如实登记）**：第一版把「快照」写成「传 `environment=` 的形态也是快照」，
实测**红了**——该形态是**按引用**持有 ⇒ 改来源**看得到**。
于是把语义按**构造方式**拆成三种（见 B），而不是把断言改松。

### B 语义面（按构造方式分三种，成对断言）

| 断言 | 核对方式 | 结论 |
| --- | --- | --- |
| 无参 `EnvCredentialResolver()`（生产路径）是**快照** | 判据用 `monkeypatch` 设值 → 构造 → `delenv` ⇒ 旧实例仍 `has()=True` | ✅ |
| **配对**：新构造的实例看到撤销 | 同上前提下 `EnvCredentialResolver().has()` 为 `False` | ✅ |
| 传 `environment=` 的形态**按引用**持有（不是快照） | 传映射 → `clear()` ⇒ 该实例变 `False` | ✅ **这条是判据第一版写错、被实测纠正的** |
| `RegistryCredentialResolver` 拷贝其映射 | 同法 ⇒ 仍 `True` | ✅ |
| 名字**大小写**：拷贝出来的是普通 dict（大小写敏感） | `has('LLM_MAIN_KEY')` 真、`has('llm_main_key')` 假 | ✅ **判据第一版就是这么红的**（Windows 上 `os.environ` 大小写不敏感，拷贝后不敏感消失） |

### C 撤销面（fail-closed + 注册表面边界）

| 断言 | 结论 |
| --- | --- |
| 来源在 ⇒ 门**开** | ✅ |
| 撤销 ⇒ 门**关**且理由**点名**凭据 | ✅ |
| 空串（存在但为空）⇒ 门**关** | ✅ |
| 已 `register()` 的 ref ⇒ 撤销环境变量**不**关该面的门 | ✅ |
| `unregister()` ⇒ 该面才关 | ✅ |
| 未注册的 ref 仍按环境快照回答（注册表只对注册过的 ref 优先） | ✅ |

### D 更正面（原假设被证伪 ⇒ 记录真的改了）

GOAL 的 EC-05 判定细则原文写「`EnvCredentialResolver` **每次读** `os.environ` ⇒
同一进程内改 `os.environ` 即生效」。**实测证伪**（撤销来源后已构造实例仍报 `True`），
本 cycle **改了 GOAL 正文**（derive 提交 `8d443ad` 内）并把实测表记进 PLAN-125。
同时补上原文**漏掉**的一条边界：**注册表命中优先于环境变量** ⇒ 撤销环境变量不是万能撤销。

### E 门禁面

- 定向套件：本判据 **20 passed**；+ 既有 runbook 判据 **30 passed**；
  `ruff check` / `format --check` / `mypy` 绿。
- 全量 m0（**CI 同形配置**：`LLM_MAIN_KEY=""` + 测试 DSN pin）⇒ 见下方「m0 结果」。
- **CI 反馈**：见下方「CI 台账」。

#### m0 结果

第一轮 **`FAILED: 1 check(s): framework/validate=1`**，红项是治理 validator：

```
Cursor 治理验证失败:
- 工程记忆来源不存在: MEM-20260920-099: .cursor/plans/rechecks/RECHECK-20260920-125-….md
```

**这是对的**：`MEM-20260920-099` 的 `source_rechecks` 指向本文件，而**本文件当时还没写**。
`python/tests` 那一侧同轮是 **4260 passed / 13 skipped**（无红）。
处置是**把本文件写出来**（记录先于引用），**不是**删掉 MEM 的来源字段或放宽 validator。
写完后的复跑结果：

**第二轮（补齐记录后，最终树）**：**`PASS: profile=m0; 23 deterministic checks`**
（`4260 passed, 13 skipped`，`616.74s`，`FAIL` 计数 **0**）——23 项全绿，治理 `validate.py` 绿。

## Warnings（不阻断，如实登记）

- **W-1 判据把「构造语义」当契约钉住。** 将来若把解析器改成「每次读环境」（一种正当重构），
  本判据会先红。这是**设计意图**（那条语义直接决定 runbook 与门的口径），但要知道：
  **改语义必须同时改 §8、GOAL 与判据**，三者是一套。
- **W-2 「按引用持有」只在测试/注入路径上成立**，判据用它**区分**两种构造方式；
  这不是缺陷，但读者容易把测试行为当成生产行为——§8 已写明「别拿它推断生产」。
- **W-3 大小写那条是平台相关事实的一半。** 判据断言「拷贝出来的 dict 大小写敏感」，
  在 Linux 上 `os.environ` 本来就大小写敏感，所以这条断言在两个平台都成立；
  但「Windows 上 `os.environ` 不敏感」这一半**没有被判据钉住**（只在 §8 与 MEM 里写明）。
- **W-4 本 cycle 没有发起任何真实调用**，也没有读、打印、写入任何真实凭据值——
  全部用虚构值驱动。**代价**：本判据**不**证明真实 `.env` 的注入链路（那是 EC-01 的 live 证据
  与 §2 的既有判据覆盖的），只证明**解析器与门的语义**。
- **W-5 可弃用额度的声明只判在场**（「免费可弃用额度」+「不放松纪律」两句），不判文笔。
- **W-6 CI 台账**：见下方小节；**未跑到终态的不记**。

## 结论

**result: PASS_WITH_WARNINGS**。EC-05 达到终态：四件事（注入 / 轮换 / 撤销 / 可弃用额度）
有**明文**且按**固定标签**可解析；「构造时快照」按**三种构造方式**钉住并**成对**断言；
撤销 ⇒ 门 **fail-closed** 关且点名凭据，注册表面必须 `unregister()` 这条边界也钉住；
**被证伪的原假设已改正**（GOAL 正文 + runbook + 判据三处一致）；判据**被压过两次**
（并在第一次压测中发现自己太松，随即收紧）。PLAN-20260920-125 可置 **DONE**。

**Warning 不阻断的理由**：W-1/W-2/W-3 是**如实说明契约范围与平台事实的边界**，
**不是**被掩盖的失败；W-4 是**刻意的范围声明**（本 EC 判语义，不判注入链路）；
W-5 是判据口径（只判在场）；W-6 是台账填写规则。
**全程未改任何门禁或断言强度**（第一次压测的处置是**收紧**判据）；**未读/未打印/未写入任何真实凭据值**；
**未新增依赖、未改 pin、未改 Policy、未发起真实调用。**
