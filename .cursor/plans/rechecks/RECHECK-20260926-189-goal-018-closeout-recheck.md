---
id: RECHECK-20260926-189
slug: goal-018-closeout-recheck
title: GOAL-018 收口独立复检（EC-04）：两树同结论 + 6/6 按压 + as-is m0 23/23 + 台账到终态
plan_id: PLAN-20260926-188
status: COMPLETED
result: PASS_WITH_WARNINGS
created_at: 2026-09-26
completed_at: 2026-09-26
owners:
  - root-agent
---

# RECHECK-20260926-189 — GOAL-018 收口独立复检

## 检查结果

**复检口径**：不复用 GOAL 的结论叙述，直接读树 + 跑判据；每一项都给出**可复核的观察面**。
**未实跑的不记通过**。

### 一、两树同结论（当前树 vs 干净 checkout）

| # | 检查 | 结果 |
| --- | --- | --- |
| 1.1 | 干净树 | `git worktree add --detach D:\rs-goal018-clean 83b782c`（cycle 1 推送 tip） |
| 1.2 | 改动面清单两树一致 | `git diff --name-only 2208dd5..HEAD` 两份清单 `diff` **无输出** |
| 1.3 | 结构半判词列 | 两树 **14/14 逐行相同**（工作树 `scratch/goal018-ec04-worktree.out` / 干净树 `…-clean.out`） |
| 1.4 | 整份输出 | **逐字节相同**（`diff` 无输出）——连明细计数也一致 |
| 1.5 | 判据面两树同结论 | 两树都是 **14 passed** / **1029 passed** / `Cursor 治理验证通过` / `DOCS-CHECK PASS` |

### 二、非恒真（按压必须判红）

| # | 按压面 | 结果 |
| --- | --- | --- |
| 2.1 | `yaml-pin`（2.8.4 → 2.8.1） | **判红**：`EC-01 yaml pin 为 2.8.4` |
| 2.2 | `undici-upgrade`（5.29.0 → 6.29.0） | **判红**：`EC-02 undici 仍锁 5.29.0 且入边恰好 1 条` |
| 2.3 | `terminal-row`（删 D-05 行） | **判红**：`EC-03 终态表 13 行且编号齐` 等三条 |
| 2.4 | `terminal-state`（已拍板为维持现状 → 待定） | **判红**：`EC-03 终态全在词汇表内且汇总 9/1/3/0` 等两条 |
| 2.5 | `goal-decl`（D-13 已实施 → 未授权待拍板） | **判红**：`EC-03 GOAL 侧 13 条声明与简报逐条同词` |
| 2.6 | `residual`（删 `R-M1` 登记行） | **判红**：`EC-04 承继残余在人工面逐条在位` |
| 2.7 | 按压是否**真的落在判据上** | 前两版**没有**：见 W-1。修后 6/6 判红 |

### 三、交付物在位

| # | 检查 | 结果 |
| --- | --- | --- |
| 3.1 | as-is 本机 m0 | **`PASS: profile=m0; 23 deterministic checks`**（`PASS [` = 24、`FAILED` 零命中、退出码 0；`scratch/goal018-c2-m0-as-is.log`） |
| 3.2 | 13 项 `D-NN` 终态表 | 13 行；终态全在四值词汇表内；重算汇总 = 已实施 **9** / 部分实施 **1** / 已拍板为维持现状 **3** / 未授权待拍板 **0** |
| 3.3 | 终态表 ↔ GOAL 人工面 | **13 条逐条同词**（两向都不缺不余） |
| 3.4 | 承继残余 | `R-M1` / `R-D1` / `R-B1` / `R-N1` / `R-F1` / `R-F2` / `W-4` / `W-5` / `W-6` **逐条在人工面节内** |
| 3.5 | CI 台账 | cycle 1 tip `83b782c`：M0 **36191382569**（`run_attempt=1`，**八 job 全 success**）+ CodeQL **36191381460**（`run_attempt=1`，3/3）+ Dependabot **36191504926**（success）；`ALL_TERMINAL` |
| 3.6 | `yaml` 告警清除（外部） | `?state=open` **9 → 8**，剩余**全部**是 `undici`（6 medium + 2 low）⇒ 同时印证 EC-02 的「一字未升」 |

### 四、边界

| # | 检查 | 结果 |
| --- | --- | --- |
| 4.1 | 鉴权 / 中间件 / 路由保护 | **零改动**（`git diff --name-only` 里 `services/api/middleware.py` / `services/api/routers/` / `services/api/app.py` 均未出现）——**甲的内容未触碰** |
| 4.2 | 依赖面 | 改动**恰为** `apps/web/package.json` + `pnpm-lock.yaml`，**无第三个**依赖文件 |
| 4.3 | `ADR-0031` 的 `Status` | **零改动** |
| 4.4 | 策略面 allow / 阈值 / 放行面 | **零改动**（`tests/egress_guard.py` 的放行面未拓宽：判据实跑 `judged 0 / blocked 0` 与 `judged 3 / blocked 0` 两种形态都未拦） |
| 4.5 | hook 面 | **零改动**（`MIMOSA_GIT_GATE_MODE` 未动；hook 仍如实报 `scanner_enobufs`） |

## 结论

**`PASS_WITH_WARNINGS`**。

- **EC-01**：`yaml` `2.8.1 → 2.8.4`（patch，`2.8.x` 最新）且**只动这一个包**；
  全量 web 门 + 根 `check` + **as-is m0 23/23** 全绿；**设计基线逐字节未改**；
  **告警面 9 → 8**（`yaml` 条目消失）。
- **EC-02**：三问各有**可复核**答案与依据，结论**可拍板**（不可在本仓正确升级 + 归属方）；
  **零升级**由两条结构性反证（`undici@5.29.0` 唯一入边未变 + 无 `overrides` 字段）钉住，
  并由告警面「剩余 8 条全是 undici」外部印证。
- **EC-03**：13 项终态齐、唯一、同词、**零「待定」**；判据**只增不减**且 **13 条按压**全部判红。
- **EC-04**：**两树同结论**（判词列 14/14 相同、整份输出逐字节相同）+ **6/6 按压判红** +
  as-is m0 终态行 + 台账到终态 + 残余逐条在位。

**Warnings（如实登记，不构成不通过）**：

- **W-1｜按压必须带作用域，判据必须绑标题行**：本轮回检**先踩到两次**同一族缺陷——
  ① `terminal-row` 与 `residual` 第一版**没判红**：简报里有一张**同形状的索引表**
  （也以 `| D-05 | …` 开头），整份替换命中了错的那张；残余检查当时扫的是**整份 GOAL**，
  而 `R-M1` 在别处还有一处提及。
  ② `_GOAL_SECTION` 的前置 `.*` 是**贪婪**的：匹配会从文件里**更早的** `##`（实测「目标与退出标准」）
  起算，把别处的编号项与终态声明一并吸进来 ⇒「13 条声明」**不是**绑在人工面节上。
  修法：按压加**作用域**（块内替换 + 块内未命中即断言失败）+ 节正则**标题行自身必须含短语**。
  **这两条已并入 `MEM-20260926-142`**（与 `MEM-20260925-141` 的「判据自身恒真」同族）。
- **W-2｜CI 台账里本轮收口提交自身的 run 只在回合汇报记账**：本记录与 GOAL 回写所在的提交
  触发的那次 run 按既有闭合约定不在文件里回填。
- **W-3｜`undici` 8 条告警原样保留**：`R-D1` 的终态表述为「`yaml` 已升、`undici` 已调研未升」；
  缓解动作在**本仓之外**（`@cursor/sdk` 上游）。**不得**把 8 → 8 读成「已处理」。
- **W-4｜`R-M1` 原样保留**：Mimosa 钩子侧 `scanner_enobufs` 仍未得完整结论
  （本轮 `commit` / `push` 的钩子都如实报了该状态）⇒ **不得**宣称项目安全。
- **W-5｜干净树的判据面用的是该提交自己的判据文件**：干净树停在 `83b782c`，
  其 `test_pending_decisions_briefing.py` 是**收紧前**的版本；两侧结论相同（都 14 passed），
  但**逐字节相同的是结构半的输出**，判据面只保证**结论同**。
