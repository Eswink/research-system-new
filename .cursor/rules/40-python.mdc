---
description: Research OS Python 模块结构、导入和函数式实现约束（作用于 M0 产品代码）
globs: "apps/**/*.py, services/**/*.py, packages/**/*.py, adapters/**/*.py, tests/**/*.py"
alwaysApply: false
---

# Python 约束

- 所有 import 放在模块顶部。除非存在严格的循环依赖且在代码中说明边界原因，不得在函数、方法或类型注解中内联 import。
- 默认使用纯函数、不可变输入输出、显式依赖和小型组合器；副作用集中在 adapter、port 实现或明确的 orchestration 边界。
- Domain 不依赖 FastAPI、SQLAlchemy、OpenHands、Temporal、Cursor 或具体 Tool Provider 类型。
- 使用稳定 enum、UTC、UUID、`Decimal` 或最小货币单位；需要 digest 的结构必须确定性序列化。
- 错误使用可分类、可测试的数据结构；禁止用裸字符串模拟 retry、policy 或状态机语义。
- 新模块必须提供与职责相称的测试；真实付费 LLM、外部网络和真实凭据不得成为默认测试依赖。

## 代码规模与复杂度阈值

以下为硬性约束，超出必须拆分或重构，禁止通过关闭 lint 规则规避。本规则仅作用于未来 M0 产品代码目录（`apps/ services/ packages/ adapters/ tests/`）。

- 行长 ≤ 100（未来 ruff 配置 `line-length = 100`）。
- 函数体 ≤ 50 行。
- 单文件 ≤ 300 行。
- 圈复杂度 CCN ≤ 10（ruff mccabe `max-complexity = 10`）。
- 函数参数 ≤ 5（超限使用参数对象/值对象）。
- 嵌套块深度 ≤ 4。

豁免：`scripts/`、`.cursor/` 下的冻结 Bootstrap 与 Cursor 治理脚本不受上述行数/CCN 阈值约束，但任何修改必须通过对应校验器（`scripts/validate_bundle.py` 或 `.cursor/skills/governance-check/scripts/validate.py`），并为新增逻辑提供测试。

未来 M0 建包时在 `pyproject.toml` 落地 ruff 与 mypy `strict`，并配置 import-linter 边界契约（见 `44-code-architecture.mdc`）；未配置前不得进入 M0 代码阶段。