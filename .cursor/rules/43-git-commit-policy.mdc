---
description: Git 硬边界：默认不提交/不推送、禁止广域暂存和破坏性历史操作；需要提交时按显式语义流程执行
alwaysApply: true
---
# Git Hard Boundaries

- **默认不创建 Git commit，也不 push。** 只有用户明确要求提交/发布/同步，或当前已授权计划明确把本地提交列为交付步骤时，才进入提交流程。
- `git push`、创建/更新远端 PR、tag/release 等外部副作用必须有与当前任务匹配的明确授权。
- 禁止 `git add -A`、`git add --all`、`git add .`；需要提交时按任务白名单路径显式暂存。
- 真实 `.env*`、私钥、凭据和任何检测出的 secret 永不进入提交；`.env.example` 等脱敏模板除外。
- 禁止 `git reset --hard`、destructive `git clean` 和改写已公开历史，除非用户明确要求且已评估影响。
- 提交前必须运行**与实际变更范围相关**的 validators/tests。只有当当前任务按风险/计划要求执行 `recheck` 时，语义提交才依赖该 recheck 的通过结果；简单、明确、无需 recheck 的任务不得被本规则反向强迫创建 recheck。
- Stop Hook 只报告工作区状态，不执行 add/commit/push（行为契约见 `.cursor/knowledge/HOOKS_REFERENCE.md` stop 行）。
- 具体暂存、Conventional Commit、验证和 rollback 流程使用显式 `semantic-commit` Skill。
