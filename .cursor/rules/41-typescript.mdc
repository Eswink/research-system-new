---
description: Research OS TypeScript、React 和联合类型穷尽处理约束（作用于 M0 产品代码）
globs: "apps/**/*.ts, apps/**/*.tsx, services/**/*.ts, services/**/*.tsx, packages/**/*.ts, packages/**/*.tsx, adapters/**/*.ts, adapters/**/*.tsx, tests/**/*.ts, tests/**/*.tsx, apps/**/*.mts, apps/**/*.cts, services/**/*.mts, services/**/*.cts, packages/**/*.mts, packages/**/*.cts"
alwaysApply: false
---

# TypeScript 约束

- 所有 import 放在模块顶部。除非存在严格的循环依赖且在代码中说明边界原因，不得在函数体、类型注解或接口字段中内联 import。
- 默认使用函数式模块、纯转换、判别联合和组合；React 使用函数组件，不在 UI 中复制后端 Canonical State。
- 对 discriminated union 或 enum 的 `switch` 必须在 `default` 分支执行 `never` 穷尽检查，使新增 variant 在编译期失败。

```typescript
const assertNever = (value: never): never => {
  throw new Error(`Unhandled variant: ${String(value)}`);
};
```

- API DTO 与 Domain 类型分离；UI 状态、OpenHands 类型和具体模型厂商不得渗入 Domain 契约。
- 状态转换集中定义并可测试，禁止在多个组件中散落字符串状态判断。
- 涉及页面或组件时同时遵循 `30-ui-skill-routing.mdc`。

## 代码规模与复杂度阈值

以下为硬性约束，超出必须拆分或重构，禁止通过关闭 lint 规则规避。本规则仅作用于未来 M0 产品代码目录（`apps/ services/ packages/ adapters/ tests/`）。

- 行长 ≤ 100（Prettier/Biome `printWidth = 100`）。
- 函数体 ≤ 50 行。
- 单文件 ≤ 300 行。
- 圈复杂度 ≤ 15（ESLint `complexity`）。
- `max-depth ≤ 4`、`max-params ≤ 3`、`max-nested-callbacks ≤ 10`。

豁免：`apps/`、`packages/` 之外的构建脚本、生成代码与 Cursor 工程脚本不受上述阈值约束，但任何修改必须通过对应校验器。

未来 M0 建包时在 `package.json`/ESLint 配置中落地上述 `max-*` 项，并配置 dependency-cruiser `forbidden` 跨层规则（见 `44-code-architecture.mdc`）；未配置前不得进入 M0 代码阶段。