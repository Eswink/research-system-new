---
description: Research OS 仓库级产品边界、架构定位和交付约束
alwaysApply: true
---

# 仓库契约

- 开始工作前先读根目录 `AGENTS.md`；再按任务读取 `README.md` 指定入口及相关 Accepted ADR、架构、安全、可靠性文档。
- 当前仓库是 v0.2.2 docs-first Bootstrap。除非任务明确要求进入 M0，不得把规划中的 `apps/`、`services/`、`packages/` 当作已经存在的实现。
- 先确定职责归属和数据流，再写功能。优先级为：模块职责清晰 > 代码表达优雅 > 功能落地。
- 发现复杂度扩散、参数爆发、状态回溯或跨层泄漏时，先调整模块边界、状态机或数据流，不在调用链上继续打补丁。
- 未确认项目使用面向对象设计时，默认采用函数式、组合式、可测试的模块；DSL 只用于提升领域可读性，不隐藏副作用。
- 不把真实 LLM 厂商、OpenHands 类型、Cursor 工程记录或上游 Runtime 类型泄漏进 Domain。
- 不回退、覆盖或格式化与当前任务无关的用户改动。遇到直接冲突时停止并询问。
- 完成任务时按 `AGENTS.md` 第 13 节报告；复杂任务还必须关联项目计划、复检和工程记忆。