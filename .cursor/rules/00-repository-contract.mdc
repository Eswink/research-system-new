---
description: Research OS 仓库级产品边界、架构定位和交付约束
alwaysApply: true
---

# 仓库契约

- 根 `AGENTS.md` 是跨工具工程契约。Cursor 已自动应用该文件时，不重复整篇加载；需要细节时按任务查阅对应章节，再读取相关 Accepted ADR、架构、安全、可靠性文档。
- 当前仓库是 v0.4.0 docs-first Bootstrap。除非任务明确要求进入 M0，不得把规划中的 `apps/`、`services/`、`packages/` 当作已经存在的实现。
- 先确定职责归属和数据流，再写功能。优先级为：模块职责清晰 > 代码表达优雅 > 功能落地。
- 发现复杂度扩散、参数爆发、状态回溯或跨层泄漏时，先调整模块边界、状态机或数据流，不在调用链上继续打补丁。
- 未确认项目使用面向对象设计时，默认采用函数式、组合式、可测试的模块；DSL 只用于提升领域可读性，不隐藏副作用。
- 不把真实 LLM 厂商、OpenHands 类型、Cursor 工程记录或上游 Runtime 类型泄漏进 Domain。
- 不回退、覆盖或格式化与当前任务无关的用户改动。遇到无法在当前授权范围内安全解决的直接冲突时，停止冲突部分并请求用户决策；其余独立工作可继续。
- Plan、Recheck、Engineering Memory 按 `20-plan-memory-recheck.mdc` 的任务规模/风险条件使用；不得为了形式创建计划、复检或伪记忆。
- 完成任务时按 `AGENTS.md` 第 14 节报告，并给出实际执行过的验证证据。
