---
description: 子代理按需委派、单波次并行上限和禁止嵌套约束
alwaysApply: true
---

# 子代理委派约束

- Subagent 是可选能力。简单任务、单文件修改或根代理可直接完成的工作不要为了形式而委派。
- 当确有并行调查、独立复核或跨模块拆分价值时，可以启动子代理。
- **每一波并行委派必须少于 4 个 child subagent，即同时最多 3 个。**
- 该限制是“单个并行 fan-out / wave”的上限，不是整个用户任务的累计上限。
- 前一波子代理全部结束、根代理完成结果整合后，如仍有新的独立必要工作，可以启动下一波；不得用连续波次制造无意义 fan-out。
- 只有直接服务用户的根代理可以创建子代理。任何子代理都禁止继续创建、委派或嵌套调用其他子代理。
- 这是 Research OS Cursor Engineering Framework 的**更严格项目策略**；Cursor 当前官方能力本身支持有限一层 nested subagent。这里主动禁用嵌套，是为了控制 fan-out、成本、审计与 Hook 可观测性，不能把它描述成 Cursor 的平台限制。
- 根代理给自定义子代理的提示必须明确禁止二次委派。
- 同一波中每个子代理职责必须互不重复；根代理负责交叉核对、最终综合和决策。
- 子代理输出默认视为调查证据，不自动成为工程事实；进入代码、Rule、Memory、ADR 前必须由根代理用仓库、测试或权威来源复核。
- **并行 wave 必须单消息批量启动**：同一 wave 的全部子代理必须在同一个 assistant 回合、同一条消息中一次性发出全部 Task 工具调用块，不得逐个启动、等待上一个完成后才启动下一个（串行化会成倍放大波次耗时）。
- 子代理之间存在真实数据依赖时属于 sequential 委派，须在回复中说明依赖关系，不得伪装成并行 wave。
- 委派前先在回复中列出该 wave 的完整清单（每个子代理的职责与输入），再在同一条消息中同时发出全部 Task 工具调用。
