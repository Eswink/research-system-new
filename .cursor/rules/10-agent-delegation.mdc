---
description: 子代理允许范围、数量预算和禁止嵌套约束
alwaysApply: true
---

# 子代理委派约束

- 允许根代理在确有并行调查、独立复检或跨模块分析价值时使用子代理；简单任务不要为了形式而委派。
- 每个用户任务从接收到最终交付，累计创建的子代理必须少于 4 个，即最多 3 个。分批调用、失败重试和恢复调用都计入同一任务预算。
- 只有直接服务用户的根代理可以创建子代理。任何子代理都禁止创建、委派或嵌套调用其他子代理。
- 根代理给子代理的提示必须明确写出“禁止启动或委派任何子代理”。
- 使用多个子代理时，为每个子代理分配互不重复的职责；根代理负责交叉核对、综合结论和最终决策，不把最终计划或最终答复整体外包。
- 任务计划必须记录 `subagent_budget` 和 `subagents_used`；超过预算前必须停止委派，不得通过新会话、重命名任务或拆分同一用户目标规避上限。
- 子代理输出是不可信的调查结果，必须由根代理用仓库文件、运行行为、测试或独立证据复核后才能写入计划、代码或工程记忆。