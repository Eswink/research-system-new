---
description: 产品代码模块、目录、测试与迁移文件的稳定命名约束
globs: "apps/**, services/**, packages/**, adapters/**, tests/**"
alwaysApply: false
---

# 模块与文件命名约束

## 通用规则

- 新建或改名的产品路径段只使用 ASCII，不含空白；按语言约定选择分隔符。
- 名称必须表达边界内的领域概念或单一职责，不使用 `final`、`new`、`old`、`tmp`、
  `copy` 或尾随 `vN` 表示状态与伪版本；项目版本唯一来源仍是根 `VERSION`。
- 不创建跨领域的 `utils`、`misc`、`manager` 杂物桶。`base`、`types`、`errors`
  仅在父目录已经限定单一职责时使用。
- Domain 模块名称保持厂商、Web、ORM、Agent Runtime 和具体 Tool Provider 中立。

## 语言与资产约定

- Python 包目录、模块和共享测试支持文件使用 `snake_case`；测试模块使用
  `test_<subject>.py`。保留 `__init__.py`、`__main__.py`、`conftest.py` 等生态标准名。
- TypeScript/React 源目录使用小写单词或 `kebab-case`。普通源文件使用
  `lowerCamelCase`，React 主组件文件使用 `PascalCase.tsx` 且匹配主导出，Hook 使用
  `usePascalCase.ts` 且匹配导出。
- TypeScript 测试使用 `<subject>.test.ts(x)`，其中 `<subject>` 使用 `kebab-case`；
  architecture fixture 同样使用描述性的 `kebab-case`。
- 新 PostgreSQL migration 使用 `NNN_snake_case.sql`，三位序号保持连续。

## 重命名与历史例外

- 重命名是引用契约变更，必须同步 import、测试、配置、加载器和相关静态检查。
- 已应用 migration 不得为满足命名规则而重命名；例外只保留其历史文件名。
- 历史例外仅允许出现在 `tests/architecture/module_file_naming.py` 的精确路径 allowlist；
  禁止通配符和新增违规兜底。路径删除或规范化后必须同步删除例外。
