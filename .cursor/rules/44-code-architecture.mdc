---
description: 产品代码的语言无关架构边界：Clean/Hexagonal 依赖方向、控制流、Port/Adapter 与副作用隔离
globs: "apps/**, services/**, packages/**, adapters/**, tests/**"
alwaysApply: false
---
# 代码架构与解耦约束

本 Rule 仅用于 Research OS 产品代码，不应在纯文档、知识库或 Cursor Framework 维护任务中长期占用上下文。

## 单一职责

- 一个模块承担一个清晰职责；复杂度扩散时优先拆模块/状态机/Port，不使用大量模式开关掩盖边界问题。
- Python/TypeScript 的量化复杂度门槛由对应语言 Rule 管理。

## 依赖方向（compile-time dependency）

```text
adapter / infrastructure → application → domain
```

- Domain 不 import application、adapter、Web/ORM/LLM/runtime/provider 等外层类型。
- Application 可以依赖 Domain，并拥有/使用对外 I/O 所需的 Port/interface。
- Adapter/Infrastructure 实现这些 Port，并依赖内层契约。

## 运行时控制流（runtime control/data flow）

不要把运行时控制流写成“domain → application → adapter”。正确区分：

```text
Inbound:
entry adapter → application use case → domain

Outbound side effect:
application → inward-owned Port → adapter / infrastructure
```

Domain 可以返回领域决策、事件或值，但**不会主动调用 Application/Adapter**，也不知道具体基础设施存在。

## Port / Adapter

- 跨外部边界（数据库、模型、工具、消息、Artifact Store、Agent Runtime）通过内层拥有的 Port 与外层 Adapter 连接。
- 禁止调用链直接穿透多层访问具体实现。
- 副作用集中在 Adapter、Port 实现或明确 orchestration 边界；业务规则尽量保持可单测。

## 反模式

- God class / 万用 utils / 状态字符串散落；
- 新 enum/union 变体不能被穷尽检查发现；
- Domain import FastAPI/SQLAlchemy/OpenHands/LiteLLM/Temporal/provider SDK；
- Application 直接实例化具体 adapter 而绕过 composition root；
- 为修局部 bug 在多层同时增加同一策略判断。

## M0 工具链

未来创建产品代码目录时，在引入业务代码前落地：

- Python：ruff + mypy strict + import-linter（或等价依赖边界测试）；
- TypeScript：ESLint + dependency-cruiser（或等价依赖/环检测）；
- CI 对 dependency boundary、lint、typecheck、test 执行确定性门禁。
