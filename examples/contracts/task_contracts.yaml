task_contracts:
  domain_discovery:
    version: 1.0.0
    purpose: Build a scoped domain map and source set.
    required_capabilities:
      - literature.search
      - literature.read
    input_schema: domain_discovery_input_v1
    output_schema: domain_discovery_output_v1
    budget:
      model_cost_usd: 12.00
      tool_requests: null
    acceptance_criteria:
      - type: SCHEMA_VALID
      - type: ARTIFACT_EXISTS
        artifact: source_set
      - type: EVIDENCE_COVERAGE
        minimum_sources: 10
    failure_policy:
      # 只声明**被消费**的键（GOAL-005 cycle 3 = EC-03）。这里曾同时写着
      # `on_validation_failure` 与 `allow_partial_evidence`，但两者都没有执行期
      # 消费者（验收门跑在任务行 durable 完成之后，改写一条 SUCCEEDED 行属 canonical
      # 状态机改动）——不生效的策略不进示例。待拍板的决策见
      # docs/adr/ADR-0030-validation-failure-consumption.md（Proposed）。用户契约里的
      # 未知键仍由 `failure_policy_view().unhonored` 点名，行为按缺省。
      on_task_failure: FAIL_RUN  # 缺省值，显式写出
    retry_policy:
      max_attempts: 3
      retryable_categories: [MODEL_RATE_LIMIT, TOOL_TIMEOUT]
      # 退避（PLAN-20260915-079）：第 n 次失败后等 min(30 * 2^(n-1), 300) 秒；
      # 这是"重排"与"再派发"之间唯一的时间间隔，没有它失败快的任务会热循环。
      backoff_seconds: 30
      max_backoff_seconds: 300

  experiment_execution:
    version: 1.0.0
    purpose: Execute a preregistered experiment in an isolated workspace.
    required_capabilities:
      - workspace.read
      - workspace.write.code
      - code.execute
      - artifact.write
    input_schema: experiment_run_input_v1
    output_schema: experiment_run_output_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: metrics
      - type: TEST_PASSES
      - type: POLICY_COMPLIANT
    idempotency_scope: experiment_spec_digest
    # GOAL-011 EC-03：本合约**声明**「这件工作不是一次会话，而是由既有沙箱实验后端跑一次实验」。
    # 声明化（而不是按合约 id 硬编码派发）是这一轮的重点：把「谁执行」写成**契约事实**，
    # 派发方才不会因为写死了一个名字而漏掉语义相同的别的合约。
    # 空映射 = 声明已给出、脚本/镜像由**装配上下文**决定（本合约是通用实验合约，
    # 跑哪个脚本属调用方的事）；`m12_experiment_execution` 则把两者**pin**在契约里。
    experiment: {}
    retry_policy:
      max_attempts: 2
      retryable_categories: [WORKER_LOST, MODEL_RATE_LIMIT]

  m12_experiment_execution:
    version: 1.0.0
    purpose: M12 controlled experiment via DockerExecutionBackend (no ToolProvider gate, workspace via backend).
    required_capabilities: []
    input_schema: experiment_run_input_v1
    output_schema: experiment_run_output_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: metrics
      - type: TEST_PASSES
      - type: POLICY_COMPLIANT
    idempotency_scope: experiment_spec_digest
    # GOAL-011 EC-03：同上——「谁执行」的声明。这一个把**脚本与镜像 pin 在契约里**
    # （M12 参考实验与 M9 沙箱镜像都是已 E2E 验证的既有资产）：装配方**不得**覆盖。
    experiment:
      script: examples/experiments/m12_reference_classification.py
      image: research-os-sandbox:m9-test
      command: python experiment.py
      timeout_seconds: 180
    retry_policy:
      max_attempts: 2
      retryable_categories: [WORKER_LOST, MODEL_RATE_LIMIT]

  console_demo_deliverable:
    version: 1.0.0
    purpose: M13-R1 console demo phase deliverable (controlled Fake agent loop output; UI 如实披露执行体性质).
    required_capabilities:
      - artifact.read
    output_schema: domain_discovery_output_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: analysis_report
      - type: EVIDENCE_COVERAGE
        minimum_sources: 1
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  real_research_deliverable:
    version: 1.0.0
    purpose: >-
      GOAL-010 EC-03：真实研究任务的交付物契约。**恰一个**声明产物（`ARTIFACT_EXISTS` 只有一条）
      ——这是 EC-01 的机制要求：声明唯一时 adapter 才把会话产出登记成合约声明的名字，
      多产物会回落事实名并判拒。**不加** `SCHEMA_VALID` / `TEST_PASSES`：今天诚实做不到的判据
      写进来只会让协议不可用（那是另一件事，不是「更严格」）。
    required_capabilities:
      - artifact.read
    output_schema: real_research_deliverable_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: analysis_report
      - type: EVIDENCE_COVERAGE
        minimum_sources: 1
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  real_retrieval_deliverable:
    version: 1.0.0
    purpose: >-
      GOAL-011 EC-02：**检索协议**（`real_retrieval_research_v1`）的交付物契约。与
      `real_research_deliverable` 的差别**只有一条**：覆盖判据多一个**来源性质**维度
      ——`minimum_retrieved_sources: 1` 要求被计的来源里**至少一条是系统取得**
      （`TrustLabel.RETRIEVED`，即运行链真的打到 provider 声明的 `network_domains`）。
      声明输入（`USER_PROVIDED`）**仍计入** `minimum_sources`，但**顶替不了**这条性质要求；
      模型自述（`GENERATED`）两条都不计入。**不动** `real_research_deliverable`：那份契约
      由没有检索的协议（`real_research_task_v1`）共用，给它加性质维度会让那些 run 全部判拒。
    required_capabilities:
      - artifact.read
    output_schema: real_research_deliverable_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: analysis_report
      - type: EVIDENCE_COVERAGE
        minimum_sources: 1
        # 性质维度：总数够还不够，**得有一条是系统取回来的**。
        minimum_retrieved_sources: 1
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  # ---- m12 参考协议其余 5 个 phase 的合约（GOAL-011 EC-03，用户拍板取 (B)）----
  # 这 5 条此前**不存在** ⇒ 运行期 `service._contract_for` 抛
  # `ValueError("phase declares no task contract")`，协议根本跑不起来（F-1）。
  # 判据一律**只声明本 phase 的输出物**（`ARTIFACT_EXISTS` 恰一个名字，与
  # `real_research_deliverable` 的先例同源）：声明唯一时 adapter 才把会话产出登记成
  # 合约声明的名字（GOAL-010 EC-01 的命名契约）；**不加** `SCHEMA_VALID` /
  # `TEST_PASSES`——今天诚实做不到的判据写进来只会让协议不可用。
  # `required_capabilities` 只取**已有 provider 且 `effect_class: READ_ONLY`** 的
  # `artifact.read`（provider `m12_artifact`）：声明 `openhands_workspace` 的
  # `workspace.*` / `code.execute` 会引出 `EXECUTE ⇒ HIGH` 的 `TOOL_RISK_ELEVATED`
  # 警告并**阻断冻结**（cycle 6 实测的阻断点）——本 cycle **不去改那个门**。

  experiment_design:
    version: 1.0.0
    purpose: m12 design phase：把领域图与来源集转成一份可执行的实验计划。
    required_capabilities:
      - artifact.read
    output_schema: experiment_plan_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: experiment_plan
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  result_analysis:
    version: 1.0.0
    purpose: m12 analysis phase：把实验产物读成观察与结论（实验已由既有 Docker 后端跑过）。
    required_capabilities:
      - artifact.read
    output_schema: validated_results_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: validated_results
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  peer_review_findings:
    version: 1.0.0
    purpose: m12 peer_review phase：对分析与结论给出逐条发现与处置建议。
    required_capabilities:
      - artifact.read
    output_schema: review_findings_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: review_findings
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  m12_research_report:
    version: 1.0.0
    purpose: >-
      m12 deliverable phase：面向读者的研究报告。**恰一个**声明产物
      （`ARTIFACT_EXISTS` 只有一条）——多产物会回落事实名并判拒。
    required_capabilities:
      - artifact.read
    output_schema: research_report_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: research_report
    retry_policy:
      max_attempts: 1
      retryable_categories: []

  m12_final_audit:
    version: 1.0.0
    purpose: m12 final_audit phase：可复现性审计（逐条检查项 + 三态结论，未知不等于通过）。
    required_capabilities:
      - artifact.read
    output_schema: audit_report_v1
    acceptance_criteria:
      - type: ARTIFACT_EXISTS
        artifact: audit_report
    retry_policy:
      max_attempts: 1
      retryable_categories: []
