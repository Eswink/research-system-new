"""Governed Memory 应用层（M10 Key Deliverable）。

- gate：MemoryWriteProposal 全链路门禁（schema → provenance → policy → curator）；
- lifecycle：Memory 生命周期（supersede / deactivate / delete / 索引协调）；
- consistency：canonical 与 derived index 的一致性检查。
"""
